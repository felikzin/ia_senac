o caba tem q sacar o `pyinstaller pra gerar o executável`, pra fazer isso o cara digita
pip install pyinstaller pelo terminal da IDE e dps `pyinstaller --onefile --noconsole app.py`.
No fim de tudo ele mostra o caminho onde o executável foi gerado e blau
